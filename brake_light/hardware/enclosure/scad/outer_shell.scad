// Render target: the lens-side outer shell (lens window frame; doubles as the
// silicone pour mould).
//   openscad -o ../stl/outer_shell.stl outer_shell.scad
include <enclosure.scad>
outer_shell();
